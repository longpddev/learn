hiển thị tooltip khi bôi đen không phải là toàn bộ như bây giờ

search được trên nhiều website chứ không phải 1 website như giờ 

lưu giữ liệu ở đâu đó như firebase chẳng hạn chứ không phải cookie như giờ 